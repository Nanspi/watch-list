
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://audiovisuel.cfwb.be/fileadmin/_processed_/b/a/csm_Aventure-jai-cinema_acf061a3d4.jpg);">
  <div class="container">
    <h1><strong><%= @list.name %></strong></h1>
    </div>
</div>







<div class="container my-3">
    <div class="cards row d-flex justify-content-center">
        <% @list.bookmarks.each do |bookmark| %>
        <div class="movie-card col-lg-3 col-sm-12 col-md-6">
        <div class="movie-image" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= bookmark.movie.poster_url %>)"></div>
          <div class="card-body">
            <h5 class="card-title"> <%= bookmark.movie.title %></h5>
            <p class="card-text"> <%= bookmark.movie.overview %></p>
            <div class="d-flex justify-content-between">
              <p class="card-comment"> <i> ❝ <%= bookmark.comment %> </i></p>
              <%= link_to '🗑️', bookmark_path, method: :delete, data: { confirm: 'Are u sure ?' } %>
            </div>
          </div>
        </div>
        <% end %>
    </div>
    <div class="d-flex justify-content-center">
            <%= link_to "let a comment", new_list_bookmark_path(@list), class: "btn btn-primary" %>
    </div>
</div>







  <%= link_to "Annihilate", list_path(@list), method: :delete, data: { confirm: "Are you sure?" } %>
  <%= link_to 'Edit', edit_list_path(@list) %> |
  <%= link_to "Back to lists", lists_path %>
